import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
  },
  topContainer: {
    flexDirection: "row",
    height: 100,
  },
  topFlatList: {
    flexDirection: "row",
    alignSelf: "center",
  },
});

export default styles;
